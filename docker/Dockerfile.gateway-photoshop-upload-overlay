ARG BASE_IMAGE
FROM ${BASE_IMAGE}

# Keep the production runtime immutable while extending the timeout only for
# the trusted Photoshop completion operation routed through scoped-api.
COPY dist/core/gateway/index.js /app/dist/core/gateway/index.js
COPY dist/core/gateway/rpc-timeout.js /app/dist/core/gateway/rpc-timeout.js
