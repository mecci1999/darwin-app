import { durableTrailsPersistenceEnabled } from './durable-persistence';

/** @deprecated Use durableTrailsPersistenceEnabled for the full durable Trails v2 composition. */
export const durableCategorySyncEnabled = durableTrailsPersistenceEnabled;
